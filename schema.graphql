type Transfer @entity {
  id: ID!
  from: String! @index
  to: String! @index
  tokenId: BigInt!
  timestamp: DateTime!
  blockNumber: Int!
  txHash: String! @index
}

type User @entity {
  id: ID!
  balance: BigInt!
  ownedToken: [Int!]!
}
